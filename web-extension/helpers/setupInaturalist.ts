/**
 * Hàm được gọi khi truy cập trang iNaturalist.
 *
 * @returns Trả về `true` nếu muốn tiếp tục thực thi các hàm con, còn không dừng lại.
 */
export function setupInaturalist(): boolean | undefined {
	return true
}
